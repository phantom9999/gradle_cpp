allprojects {
    apply plugin: 'xcode'
    apply plugin: 'maven-publish'

    group = 'org.phantom9999.googletest'
    version = '1.2'

    publishing {
        repositories {
            maven {
                url("file:../../local_maven/repository")
            }
        }
    }
}

